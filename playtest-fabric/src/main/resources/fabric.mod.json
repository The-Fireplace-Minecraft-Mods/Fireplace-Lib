{
  "schemaVersion": 1,
  "id": "flplaytest",
  "version": "${version}",
  "name": "${readable_name} Playtest",
  "description": "Dev environment test suite for ${readable_name}",
  "authors": [
    "The_Fireplace"
  ],
  "contact": {
    "homepage": "${modrinth_url}",
    "sources": "${github_url}",
    "issues": "${github_url}/issues",
    "curseforge": "${fabric_curseforge_url}"
  },
  "license": "${license_id}",
  "icon": "",
  "environment": "*",
  "entrypoints": {
    "di-main": [
      "dev.the_fireplace.playtest.entrypoints.Main"
    ]
  },
  "depends": {
      "fabricloader": ">=0.12.0",
      "minecraft": "*",
      "annotateddi": "*",
      "fireplacelib": "${version}"
  }
}
